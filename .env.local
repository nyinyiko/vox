# VoxCPM2 production endpoint. Keep this on the managed local server so Thalika controls
# model version, inference serialization, sampling seed, inference steps, and retries.
HF_VOXCPM2_URL=http://localhost:7860
HF_REQUEST_TIMEOUT=60000
HF_INFERENCE_TIMEOUT=300000
# Stability knobs (tune by ear — see VoxCPM #302/#272):
# VOXCPM_CHUNK_CHARS: chars per chunk. Bigger = fewer takes (less segment-to-segment variation) but
#   long takes drift mid-take; smaller = more uniform takes but more cross-chunk variation. Default 180.
# VOXCPM_CHUNK_CHARS=180
# VOXCPM_RETRY_RATIO (local server): lower = retry degenerate takes more aggressively. Model default 6.0.
# VOXCPM_RETRY_RATIO=4.0
# VOXCPM_DEVICE=auto  # auto / mps / cuda / cpu
# VOXCPM_TIMESTEPS=10  # higher can improve quality but increases generation time
# VOXCPM_LOAD_DENOISER=1  # load the reference denoiser (the `denoise` toggle); ~30s slower, MPS may crash.
# QA only: keep an un-mastered "_raw" sibling of each new generation so History shows a
# raw-vs-mastered A/B player on identical content. Unset/0 in normal use.
# THALIKA_KEEP_RAW_OUTPUT=1
GEMINI_REQUEST_TIMEOUT=60000
GEMINI_API_KEY=your_google_gemini_api_key_here
